// Makes /dashboard/album/[id] a real route so permalinks and refreshes
// resolve; the visible card comes from the @information slot's fallback page.
export { default } from "../../@information/album/[id]/page";
