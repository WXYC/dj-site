import CatalogExperience from "@/src/components/experiences/modern/catalog/CatalogExperience";

/** Catalog shell shown under the album detail modal on direct loads and refresh. */
export default function CatalogAlbumPage() {
  return <CatalogExperience />;
}
