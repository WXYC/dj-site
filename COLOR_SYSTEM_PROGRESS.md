# Color System — implementation progress

Branch: `feat/color-system` (off `feat/flowsheet-entry-redesign`; rebase onto main once the entry redesign lands).
Dev server: watch on http://localhost:3000.

Two-tier semantic color system for the **modern** experience + a theme-gallery
picker. Classic experience stays untouched and strippable.

- **Layer A** — semantic palette CSS vars (`sidebar`, `sidebarAdmin`,
  `formatVinyl`, `formatCd`, `exclusive`, `onAir`, `rotation.*`) generated by
  `extendTheme`, consumed via `theme.vars.palette.*` / `color=` props.
- **Layer B** — a typed role→`{color,variant}` map (`tokens/roles.ts`) for Joy
  component props (genre/status/entry/etc.).
- Adding a theme = one file in `themes/definitions/` + one line in `themes/index.ts`.

## Steps

- [x] **1. Token + theme-definition infrastructure** — `themes/{types,tokens,buildTheme,index}.ts`,
      `themes/definitions/{default,ocean}.ts`, `tokens/roles.ts`; `modern/theme.ts`
      → thin re-export; deleted dead `shared/base-theme.ts` (+test); updated
      `theme.test.ts`; added `themes.test.ts`. tsc clean, 51 tests pass, app compiles (200).
      - Verified in node_modules that custom palette keys need **explicit** variant
        tokens (`createVariantPalette`) — Joy does not auto-generate them from a bare scale.
- [ ] 2. Preference plumbing (`themeId` end-to-end, back-compat)
- [ ] 3. Server-driven ThemeRegistry + ModernThemeContext (fix classic theme selector)
- [ ] 4. Theme picker UI (Appbar dropdown)
- [ ] 5. Catalog migration (genre/format/exclusive + prefix-bug fixes)
- [ ] 6. Flowsheet + widgets migration (entries, SmartEntry, GoLive, NowPlaying, bins, rotation)
- [ ] 7. Sidebar decoupling (LeftbarContainer + Logo)
- [ ] 8. Admin roster tones + dead-code sweep
- [ ] 9. Verify pass (vitest + e2e + manual screenshots; classic QA)

## Decisions / notes

- Formats get **own hues** (default theme: amber=vinyl, sky=CD), not primary/warning.
- Preference grammar: `modern-<mode>` (legacy, themeId=default) and
  `modern-<themeId>-<mode>`; emit 2-part when default so old clients keep working.
- Base branch is moving (other agent on `flowsheet-entry-redesign`) — merge from it
  periodically; migrate its SmartEntry hardcoded colors here.
